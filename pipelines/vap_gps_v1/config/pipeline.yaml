classname: pipelines.vap_gps_v1.pipeline.VapGPS
parameters:
  datastreams:
    - clallam.spotter-gps-1min.a1

triggers: []

retriever:
  path: pipelines/vap_gps_v1/config/retriever.yaml

dataset:
  path: pipelines/vap_gps_v1/config/dataset.yaml

quality:
  path: shared/quality.yaml
storage:
  path: shared/storage.yaml
