classname: pipelines.vap_wave_raw_v1.pipeline.VapWaves
parameters:
  datastreams:
    - clallam.spotter-pos-400ms.a1

triggers: []

retriever:
  path: pipelines/vap_wave_raw_v1/config/retriever.yaml

dataset:
  path: pipelines/vap_wave_raw_v1/config/dataset.yaml

quality:
  path: pipelines/vap_wave_raw_v1/config/quality.yaml

storage:
  path: shared/storage.yaml
